# Dry-run — SDLC reliability loop (2026-08-12)

## Goal

Validate gap → prompt → gate evidence → prompt regeneration **without** promoting or falsely clearing matrix rows.

## Results

| Tick | Gap | Prompt | Evidence overall | Matrix write-back |
|---|---|---|---|---|
| 1 | REQ:ADR-003-R6 | Generated | **fail** (coverage still open) | No `pass` (correct) |
| 2 | MATRIX:native-host:AC4 | Regenerated from backlog | **fail** | No `pass` (correct) |
| 3 | MATRIX:marketplace:L3 | Regenerated again | **fail** | No `pass` (correct) |

## Verified behaviors

1. `extract-requirements.ps1` + `check-spec-coverage.ps1` produce open P0 list (10).
2. `next-prompt.md` is overwritten each tick with a new gap objective (not reused prose).
3. Evidence lives under `Sessions/loop/evidence/<tick>/gates.json` with exit codes.
4. Gaps do **not** shrink without overall=pass evidence (matrix unchanged for AC4 / L3 / ADR-003-R6).

## Next real ticks

Automation / `/sdlc-loop` should implement the top gap (Maestro smoke / calendar / marketplace L3) and only then run write-back to `pass`.
