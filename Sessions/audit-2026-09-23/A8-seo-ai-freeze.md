## A8 — SEO, contenuti, AI, feature in freeze

Perimetro analizzato (solo lettura, 2026-09-23): backend `/home/user/backend` (Casazen.Web / Core / Infrastructure / Tests), frontend `/home/user/frontend` (src, e2e, public, vercel.json, index.html), mobile `/home/user/mobile` (nessun codice SEO/AI/freeze trovato). Riferimenti di piano: `Sessions/specs/spec-seo-funnel.md` (US-026 / #300), spec FREEZE (ai-copilot-messaging, unified-inbox, google-vacation-rentals, enterprise-scale, eu-compliance-es-fr, supplier-marketplace), `Sessions/PLANNING.md` §"SEO comuni + funnel" e §"Freeze (con eccezione iCal)", `docs/AI-STRATEGY.md`, `docs/INFRA.md`.

Tutti i percorsi senza prefisso sono relativi a `backend/` (BE) o `frontend/` (FE) come indicato.

Legenda stato: **DONE** / **PARTIAL** / **STUB** (dati hardcoded, mock, TODO) / **MISSING** / **BROKEN**. Per le feature in freeze: **NON INIZIATO (ok)** / **INIZIATO E ESPOSTO (problema)** / **INIZIATO E NASCOSTO**.

---

### 1. Copertura del piano

#### 1a. `spec-seo-funnel.md` (US-026, #300 — P0 MVP, registro "planned", frontmatter "specced")

| ID | Punto pianificato (spec/AC/issue) | Stato | Evidenza (file:riga) | Note |
|---|---|---|---|---|
| SF-AC1 | Riuso entity `SeoContentPage` + API pubbliche di #258 | DONE (prerequisito) | BE `Casazen.Core/Entities/SeoContentPage.cs:7-44`; `Casazen.Web/Controllers/PublicContentController.cs:23-53`; migrazione `Casazen.Infrastructure/Migrations/20260611121742_AddComplianceSeoEntities.cs:14-92` | La base esiste, ma è piena di difetti (vedi 1b e A8-04…A8-09). Nel funnel non è stato scritto codice nuovo: `git log` BE mostra solo commit #258/#263/#266, nessuno per #300. |
| SF-AC2 | `GET /api/public/seo/{comuneSlug}` → `{title, content, meta, featuredProperties[], cta}` | MISSING | Nessun `PublicSeoController`/route `api/public/seo` (grep vuoto). `Casazen.Core/DTOs/PublicPropertyDto.cs:5-25` non ha `OrgSlug` → impossibile linkare `/book/{orgSlug}/property/{slug}` | Il filtro "Active compliance" esiste già (`Casazen.Infrastructure/Repositories/PropertyRepositories.cs:38-56`, filtro a riga 41) ed è riusabile. |
| SF-AC3 | `POST /api/public/seo/events` anonimo, senza PII | MISSING | Nessuna entity `SeoEvent`, nessuna migrazione, nessun endpoint (grep `SeoEvent|seo/events` vuoto su BE e FE) | README.md:123 segnala già il dubbio su IP come PII. |
| SF-AC4 | Rotta `/destinazioni/{comune}` (o path esistente) con `PublicSiteShell` + token design system | PARTIAL | FE `src/routes/index.tsx:141-147` (le pagine `/p/...` sono dentro `PublicSiteShell mode="default"`) | Le pagine usano token della console (`text-muted-foreground`, `bg-muted/40`, `compliance-guide-page.tsx:46-67`, `seo-cta-block.tsx`), nessun hero/gallery/componenti del design system pubblico. |
| SF-AC5 | CTA primaria "Pubblica la tua casa" → `/signup?comune={slug}`; secondaria: card immobili → sito host | BROKEN | CTA generate in BE `Casazen.Infrastructure/Services/SeoContentService.cs:494-497`; FE `src/features/public-seo/components/seo-cta-block.tsx:25,30`; nessuna rotta `/signup` né `/tools/verifica-conformita` in `src/routes/index.tsx:112-193` | Il click finisce su `/login` e perde UTM e comune (A8-03). Testo "Registrati gratis" anziché "Pubblica la tua casa". Card immobili assenti. |
| SF-AC6 | Sezione tassa di soggiorno se esiste `TouristTaxRate`; avviso + link se manca | PARTIAL | Tariffa mostrata se presente: `tourist-tax-calculator-widget.tsx:68-72`. Se manca: widget comunque mostrato (`compliance-guide-page.tsx:60-63`), calcolo → 404 (`SeoContentService.cs:59-61`) → errore generico (`tourist-tax-calculator-widget.tsx:151-155`) | Nessun avviso e nessun link. Nessun seed `TouristTaxRate` nelle migrazioni. |
| SF-AC7 | Layout mobile-first; LCP come public site AC4 (< 2.5 s su 4G) | PARTIAL (non verificato) | Layout `max-w-3xl px-4` (`compliance-guide-page.tsx:46`); nessun SSR/prerender (`package.json`, `vite.config.ts` senza plugin); contenuto disponibile solo dopo bundle JS + chiamata API Railway | Nessuna misura LCP in repo. `prose` non ha effetto (A8-20). |
| SF-AC8 | Click CTA → evento AC3 + Plausible/GA opzionale | MISSING | Nessun `useSeoEvent`, nessuno script Plausible/GA (grep `plausible|gtag` vuoto in `src`) | — |
| SF-AC9 | Widget admin "top comuni per click CTA (30 gg)" + API | MISSING | `src/features/admin/admin-dashboard-page.tsx`: nessun riferimento a SEO/CTA | — |
| SF-AC10 | Pilota: ≥ 3 comuni con contenuti completi + immobili in evidenza | STUB | Registry di 12 comuni hardcoded `Casazen.Core/Regulatory/ItalianComuneRegistry.cs:13-26`; contenuto di default = una frase dello stub `Casazen.Infrastructure/External/StubAiProvider.cs:33`; nessun immobile in evidenza | Con `Ai:Provider` assente (nessuna sezione `Ai` in `appsettings.json`) il provider è Stub (`AiProviderRegistration.cs:93-97`). |
| SF-UX | UX/UI Quality: stato errore distinto, stringhe italiane, nessun vicolo cieco | PARTIAL | Errore API e "non trovato" coincidono (`compliance-guide-page.tsx:34-43`, `tourist-tax-calculator-page.tsx:34-43`); CTA porta a un vicolo cieco (A8-03) | — |
| SF-TEST | Test per ogni AC (L1/L2/L3), AC Test Map | MISSING | Nessun test del funnel; E2E `e2e/compliance-seo.spec.ts` cancellato nel commit FE `7b53a08` (2026-06-23) | Vedi A8-18. |

#### 1b. Base SEO #258/#263/#266 e voci PLANNING "SEO comuni + funnel"

| ID | Punto pianificato | Stato | Evidenza (file:riga) | Note |
|---|---|---|---|---|
| B-01 | API pubblica guida `affitti-brevi/{region}/{comune}` | DONE | `PublicContentController.cs:23-38`; `SeoContentRepository.cs:19-40` | In Production vengono servite solo le pagine Reviewed (`PublicContentController.cs:20-21`). |
| B-02 | API pagina tassa + calcolatore pubblico | PARTIAL | `PublicContentController.cs:40-69`; `SeoContentService.cs:51-90`; `TouristTaxService.cs:40-41` | I bambini sono sempre esenti, a prescindere da `MinimumAge`. Il rate limiter è globale (A8-22). Lookup case-sensitive (A8-23). |
| B-03 | Generazione contenuti "AI" per comune (job Hangfire) | STUB | `SeoPageGenerationJob.cs`; `SeoContentService.cs:291-364`; prompt senza istruzioni `SeoContentService.cs:455-469`; Stub `StubAiProvider.cs:33` | Con DeepSeek senza chiave si ottiene un body vuoto (`DeepSeekAiProvider.cs:30-33`). A8-06. |
| B-04 | Revisione legale / counsel sulle prime 100 pagine | BROKEN | Controllo in `SeoContentService.cs:127-131`, aggirato da `SeoBootstrapOptions.cs:11`, `SeoPageGenerationJob.cs:33-35`, `SeoAdminDtos.cs:5`, FE `seo-dashboard-page.tsx:44,140`, `admin-seo.api.ts:45` | A8-04. |
| B-05 | Budget AI piattaforma (`PlatformAiBudget`) | BROKEN | `PlatformAiBudget.cs:86-88`; `SeoContentService.cs:324-336`; `DeepSeekAiProvider.cs:54` | A8-07. |
| B-06 | Refresh mensile delle pagine obsolete | PARTIAL | `Program.cs:435-439`; `SeoContentService.cs:226-256`; `SeoContentRepository.cs:146-170` | Il testo rigenerato va online senza revisione (A8-05). |
| B-07 | Sitemap delle pagine compliance | BROKEN | `ComplianceSitemapController.cs:14-20`; `SeoContentService.cs:258-289` | Servita solo dall'host API. Gli URL puntano a `www.casazen.it` (A8-02). |
| B-08 | Meta/canonical/indicizzabilità (SPA Vite) | BROKEN | FE `index.html:2` (`lang="en"`), `index.html:7` (`<title>temp-vite</title>`); `src/lib/seo-meta.ts:29-49`; nessun SSR/prerender, nessun `robots.txt` (`public/` = `favicon.svg`, `icons.svg`); `vercel.json:5` | A8-02, A8-09. |
| B-09 | Sanitizzazione dell'HTML AI | PARTIAL | BE `SeoHtmlSanitizer.cs:10-28`; FE `src/lib/sanitize-html.ts:51-57` | Blacklist regex aggirabile (A8-08). |
| B-10 | Dashboard admin SEO (lista, genera, approva, budget) | PARTIAL | FE `src/features/admin/seo-dashboard-page.tsx:26-181`; BE `AdminSeoController.cs:12-131` (policy `AdminOnly` riga 14 ✔) | Mancano anteprima, ritiro, paginazione, stato errore del budget (A8-21). |
| B-11 | "Free Compliance Checker" (CTA `/tools/verifica-conformita`) | MISSING | `SeoContentService.cs:496`; nessuna rotta FE né endpoint BE | Presente in `docs/AI-STRATEGY.md:243-245` come lead magnet. |
| B-12 | PLANNING.md:201 "Pagine SEO comune: Backend OK, FE da allineare al design system + CTA" | PARTIAL | Vedi SF-AC4/AC5 | L'affermazione "Backend OK" di PLANNING non è vera (B-03…B-07). |
| B-13 | PLANNING.md:262: wizard step 5, tariffa assente → avviso + link alla pagina SEO | MISSING | FE `src/features/compliance/activation-wizard.tsx:264` punta a `/app/admin/compliance/tax-rates`, rotta admin inaccessibile all'host | A8-28 (cross-area). |
| B-14 | PLANNING.md:194 "SSR/SSG dove possibile (Vercel)" | MISSING | Nessun prerender. `vercel.json:5` fa un rewrite totale su `index.html` | — |
| B-15 | Ricerca pubblica `/search` (destinazione del link "Esplora" nello shell SEO) | BROKEN | `PublicSiteShell.tsx:120-121`; `search-page.tsx:24-26,31`; `search-filters.tsx:65-116`; `search.schema.ts:5-10` | A8-13. |
| B-16 | SEO del sito booking (branded-booking-site AC12-14: JSON-LD, meta/og, sitemap org + robots), dichiarato "shipped" | MISSING | `useSeoMeta` è usato solo in `src/features/public-seo/*` (grep); nessun `application/ld+json`; nessun endpoint sitemap org (grep `sitemap` BE → solo `ComplianceSitemapController`) | È la destinazione del funnel SEO. A8-29 (cross-area). |

#### 1c. Funzionalità AI

| ID | Punto | Stato | Evidenza (file:riga) | Note |
|---|---|---|---|---|
| AI-01 | Astrazione `IAiProvider` (tier Economy/Frontier) | PARTIAL | `Casazen.Core/Services/IAiProvider.cs`; `AiProviderRegistration.cs:89-104` | DeepSeek ignora il parametro tier. Nessuna policy Polly/retry (solo `Timeout` 30 s, `AiProviderRegistration.cs:99`). Registrazione duplicata (A8-26). |
| AI-02 | Budget/cap dei costi AI | BROKEN | `DeepSeekAiProvider.cs:54` (token=0); `SupplierMatchService.cs`, `AiSupplierDiscoveryService.cs` non consultano il budget | A8-01, A8-07. |
| AI-03 | "AI Supplier Discovery" (DeepSeek + web search) | INIZIATO E ESPOSTO (non in spec, visione US-014 in freeze) | `AiSupplierDiscoveryService.cs:25-63`; `DeepSeekWebSearchClient.cs:16-56`; UI `service-request-form.tsx:207-240` | Viene presentata come dati "Google" (A8-14). |
| AI-04 | Motivazione AI del match fornitore | INIZIATO E ESPOSTO | `SupplierMatchService.cs:97-130`; trigger automatico FE `service-request-form.tsx:68-76` | Le note dell'host vanno a DeepSeek (A8-15). |
| AI-05 | "Prezzi AI" (pricing dinamico) | STUB (motore a regole etichettato AI) | `PricingAdapterService.cs:27,147,149`; i18n `it.json:914,935,1005,1129` | Il documento `docs/AI-STRATEGY.md:14-30` riconosce già il rischio di pubblicità ingannevole (A8-17). |
| AI-06 | Trasparenza AI Act (`AiContentNotice`) | STUB | `src/components/shared/ai-content-notice.tsx` usato solo nei test | A8-27. |

#### 1d. Feature in FREEZE (PLANNING.md:523-527)

| ID | Spec / voce freeze | Stato | Evidenza (file:riga) | Note / azione |
|---|---|---|---|---|
| F-01 | `spec-unified-inbox` (US-011) | NON INIZIATO (ok) | Grep `Conversation|InboxController|api/inbox` su BE: 0 risultati. FE: solo `supplier-inbox-page.tsx` (console fornitore MVP, `route-manifest.ts:543-551`) | La "inbox" fornitore è in scope (US-022) e non va confusa con la US-011. |
| F-02 | `spec-ai-copilot-messaging` (US-012) | NON INIZIATO (ok) per il messaging | Grep `AiMessaging|AiMessageLog|ai-draft|ai-messaging`: 0 risultati | L'infrastruttura AI generica (DeepSeek) però esiste ed è usata in funzioni non specificate (AI-03/AI-04). |
| F-03 | `spec-google-vacation-rentals` (US-015) | NON INIZIATO (ok) | Grep `Gvr|VacationRental|api/distribution` BE e `distribution|gvr` FE: 0 risultati | — |
| F-04 | `spec-enterprise-scale` (US-016) | NON INIZIATO (ok) | Grep `Brand|BrandId|EnterpriseController|SlaMetric` BE e `enterprise|sso-config` FE: 0 risultati | — |
| F-05 | `spec-eu-compliance-es-fr` (US-017) | NON INIZIATO (ok) | Grep `IComplianceMarketResolver|IRegistrationNumberValidator|IGuestReportingService`: 0 risultati. `VatCalculationService.cs:43-45` (codici UE per l'IVA) non è correlato | — |
| F-06 | `spec-supplier-marketplace` full (US-014) | Connect/take-rate/DAC7: NON INIZIATO (ok). Pilastri AI A/B di AI-STRATEGY: INIZIATO E ESPOSTO | `StripeService.cs:69,214` `ApplicationFeeAmount = 0`; nessun `MarketplaceOrder`/`Dac7`. AI: AI-03/AI-04 | La pagina `/app/short-rent/marketplace` (`route-manifest.ts:231-240`) è il micro-marketplace v0 (MVP) e va bene. Il discovery AI esterno va messo dietro flag. |
| F-07 | OTA API partner #31-35 (non iCal) | INIZIATO E ESPOSTO | Voce di menu `route-manifest.ts:318-334`; il form raccoglie API key/secret (`src/features/ota/ota-setup-page.tsx:128-143`), salvati in chiaro (`Casazen.Core/Entities/OtaIntegration.cs:26,30`); job `ota-sync-all` orario e `booking-pull-all` ogni 15 min con `Guid.Empty` (`Program.cs:381-391`) → no-op + warning a ogni esecuzione | A8-16. Nascondere dietro feature flag e disattivare i job. |
| F-08 | LTR #269 (US-007…010, "Closed — do not resume") | INIZIATO E ESPOSTO + sviluppo attivo | Onboarding offre `LongTerm`/`Both` (`src/features/onboarding/onboarding-page.tsx:20`); contesto long-rent `route-manifest.ts:359-384`; job `Program.cs:417-433`; commit BE di agosto 2026 `ed76c3f` (RLI US-010), `ea23ff8`, `f4687e3` (US-009) | A8-16. Il freeze non viene rispettato mentre il funnel SEO (P0) non è partito. |

---

### 2. Bug e difetti

| ID | Sev. | Dove (file:riga) | Descrizione | Scenario di fallimento concreto | Fix proposto | Effort |
|---|---|---|---|---|---|---|
| A8-01 | **P0** | BE `Casazen.Web/Controllers/ServiceRequestsController.cs:21-47`; `SupplierMatchService.cs:47-51,108-119`; `AiSupplierDiscoveryService.cs:30-56`; `DeepSeekWebSearchClient.cs:29-31`; `DeepSeekAiProvider.cs:54` | **Denial-of-wallet AI.** `POST /api/service-requests/match-supplier` è aperto a ogni utente autenticato, senza rate limit, con `Category` libera (solo `Trim`, riga 41). Senza fornitori in zona parte una web search DeepSeek (`max_tokens` 4096 + tool `web_search`) più un'estrazione LLM (`max_tokens` 2048). Nessun budget viene consultato. La cache si scrive solo se ci sono risultati (riga 53-54). Il provider riporta 0 token. | Un utente free crea una property e lancia uno script `POST match-supplier` con `category=<uuid casuale>`: ogni richiesta fa 2 chiamate LLM a pagamento, senza limite né allarme. Anche l'uso normale rilancia il match a ogni cambio di categoria o urgenza (`service-request-form.tsx:68-76`). | Allowlist delle categorie (enum lato BE). Rate limit per utente/org (`AddPolicy` partizionato come `PublicResolveHost`, `Program.cs:159-167`). `AiBudgetGuard` centrale con cap mensile per org e per piattaforma, controllato **prima** della chiamata. Lettura di `usage.prompt_tokens/completion_tokens` dalla risposta DeepSeek. Cache anche dei risultati vuoti (TTL 24 h). Feature flag per disattivare il discovery esterno (freeze US-014). | M |
| A8-02 | **P0** | BE `SeoContentService.cs:261,473` (default `https://www.casazen.it`); `docs/INFRA.md:365-394` (manca `Seo__PublicBaseUrl`), `INFRA.md:442` (FE prod = `casazen-app.vercel.app`); CORS `ServiceCollectionExtensions.cs:215-223`; `ComplianceSitemapController.cs:14`; FE `vercel.json:5`, `public/` | **Le pagine SEO non sono scopribili sul dominio reale.** Canonical e sitemap puntano a `www.casazen.it`, che non è l'host FE documentato e non è tra le origini CORS. La sitemap esiste solo sull'host API Railway: è cross-host e nessun `robots.txt` la dichiara. Il FE non ha `robots.txt` né sitemap: il rewrite totale restituisce `index.html` con 200 per `/robots.txt` e `/sitemap.xml`. Nessuna pagina interna linka `/p/*` (grep). | Googlebot scarica `https://casazen-app.vercel.app/robots.txt` e riceve HTML. La sitemap su `*.railway.app` elenca URL `www.casazen.it` che non possono esservi dichiarati. Se `www.casazen.it` viene puntato alla SPA, tutte le chiamate API falliscono per CORS e la pagina mostra "Pagina non trovata". Se non viene puntato, il canonical indica un dominio morto. In entrambi i casi le 24 pagine non vengono indicizzate. | Decidere il dominio canonico (ADR), poi impostare `Seo__PublicBaseUrl` e aggiungerlo a CORS e Vercel. Pubblicare `public/robots.txt` con `Sitemap:` e servire la sitemap dal dominio FE (rewrite Vercel `/sitemap.xml` → API, oppure generazione al build). Pagina hub `/p/affitti-brevi` con link a tutti i comuni e link dal footer pubblico. | M |
| A8-03 | **P0** | BE `SeoContentService.cs:494-497`; FE `seo-cta-block.tsx:25,30`; `src/routes/index.tsx:112-193`; `catch-all-redirect.tsx:12`; `protected-route.tsx:40-42`; `src/contexts/auth-bridge.tsx:86-88` | **Il funnel CTA è rotto.** `/signup` e `/tools/verifica-conformita` non esistono. Il catch-all porta a `/`, che sta dentro `ProtectedRoute` e rimanda a `/login`. Su un path pubblico il bridge anonimo fa `login()` → `window.location.assign('/login')`: serve un reload e un secondo click, poi si arriva al **login** Auth0 (non al signup). `utm_*` e `comune` vanno persi. Manca ogni attribuzione lato BE (grep `utm|attribution` vuoto). | Il visitatore da Google clicca "Registrati gratis" su `/p/affitti-brevi/lombardia/como`, vede "Accedi", deve cliccare 2 volte e arriva al form di login. Molti abbandonano. La conversione per comune non è misurabile: l'obiettivo "quali comuni convertono" di US-026 non è raggiungibile. | Rotta pubblica `/signup` che chiama `loginWithRedirect({ authorizationParams:{ screen_hint:'signup' }, appState:{ comune, utm } })`. Persistenza dell'attribuzione in onboarding (campi `SignupSource/Comune/Utm*` su Org/User + migrazione). Rimuovere o nascondere la CTA "Verifica conformità" finché il tool non esiste. Testo CTA come da spec. | M |
| A8-04 | P1 | BE `appsettings.json:97-100`; `SeoBootstrapOptions.cs:11`; `SeoBootstrapHostedService.cs:27-38`; `SeoPageGenerationJob.cs:33-35`; `SeoAdminDtos.cs:5`; `ISeoContentService.cs:26-29,31-35`; FE `seo-dashboard-page.tsx:44,57,135-142`; `admin-seo.api.ts:45`; `use-admin-seo.ts:28` | **Il gate di revisione legale è un timbro automatico.** Al primo deploy il bootstrap genera e auto-approva le pagine con `counselApproved:true`. "Approva tutte" ha default `true` sia in BE sia in FE. Il pulsante di riga invia sempre `counselApproved:true`. Il DTO admin non contiene il body: l'admin approva senza vederlo. Nessun audit (chi, quando, quale revisione). | Deploy su DB vuoto: 24 pagine su CIN, Alloggiati e tassa di soggiorno sono pubblicate e in sitemap senza revisione umana, anche se il vincolo `CounselRequired` (prime 100 pagine) doveva impedirlo. | `AutoApproveAfterBootstrap=false` e `BootstrapOnStartup=false` in prod. Eliminare i default `true` (DTO + FE). Checkbox esplicita "Revisione legale effettuata". Anteprima del body nell'admin. Campi `ApprovedByUserId`, `ApprovedAt`, `ApprovedRevisionId` + migrazione. | M |
| A8-05 | P1 | BE `SeoContentRepository.cs:146-170`; `SeoContentService.cs:226-256,338-361,371`; `Program.cs:435-439` | **La rigenerazione ripubblica testo mai revisionato.** L'upsert non rimette la pagina in Draft. La nuova revisione viene servita subito, perché `MapPublicPageAsync` legge l'ultima revisione (riga 371). | L'admin aggiorna la tariffa di Como. Il 1° del mese il job di refresh rigenera con l'LLM e il nuovo testo (mai visto da nessuno) va online su una pagina "Reviewed". | Alla nuova revisione: `LegalReviewStatus=Draft` e pagina servita sulla **revisione approvata** (`PublishedRevisionId`). La nuova revisione resta pending fino all'approvazione. | S |
| A8-06 | P1 | BE `AiProviderRegistration.cs:93-97`; `StubAiProvider.cs:33`; `DeepSeekAiProvider.cs:30-33`; `SeoContentService.cs:455-469,482-492`; `docs/INFRA.md:389` | **Contenuti stub, vuoti o fuori controllo pubblicati.** Senza la sezione `Ai` il provider è Stub: una frase del tipo "Como: contenuto generato per affitti brevi…". Con `Ai__Provider=DeepSeek` (consigliato da INFRA.md) ma chiave assente, il body è vuoto. Con la chiave, il prompt contiene solo dati, senza istruzioni su lingua, formato HTML, struttura, lunghezza o fonti. Il disclaimer "Contenuto generato con AI" compare anche sullo stub. | In prod le pagine indicizzabili hanno una riga di testo (thin content, rischio penalizzazione "scaled content"). Con DeepSeek l'output può essere markdown o una risposta del tipo "Sembra che tu abbia fornito…", che viene auto-approvata (A8-04). | Prompt di sistema versionato (IT, HTML con allowlist, sezioni obbligatorie CIN/Alloggiati/tassa/fonti ufficiali, niente consigli legali). Validazione dell'output (lunghezza minima, tag ammessi, niente markdown): se fallisce, non salvare. Rifiuto dei body vuoti. Disclaimer coerente con il provider effettivo. | M |
| A8-07 | P1 | BE `DeepSeekAiProvider.cs:54`; `SeoContentService.cs:155-189,324-336`; test `SeoContentServiceTests.cs:66-105` | **Il budget AI piattaforma non ha effetto.** DeepSeek restituisce `PromptTokens=0` e `CompletionTokens=0`, quindi `TokensUsedThisMonth` non sale mai. Il controllo avviene **dopo** la chiamata e conta solo i prompt token. Al superamento fa `return false` ma il ciclo prosegue e chiama il modello per ogni pagina restante, scartandone il risultato. Il test verifica solo che la revisione non venga salvata. | Batch "Genera tutti" a budget esaurito: 24 chiamate pagate e nessuna pagina salvata. Con DeepSeek la card "Budget AI piattaforma" mostra sempre 0 / 500.000. | Parsing di `usage` dalla risposta. Stima preventiva e `break` del batch a budget superato. Conteggio di prompt + completion. Test che verifichi `aiProvider.GenerateAsync` `Times.Never` oltre il cap. | S |
| A8-08 | P1 | BE `SeoHtmlSanitizer.cs:15-28`; FE `src/lib/sanitize-html.ts:51-57`; `compliance-guide-page.tsx:56`; `tourist-tax-calculator-page.tsx:55`; `src/config/auth.config.ts:11-12` | **XSS latente.** Il sanitizer è una blacklist regex. Verificato eseguendo la regex FE: `<img src="x"/onerror="alert(1)">` e `<a href="&#106;avascript:alert(1)">` passano **invariati** (la regex BE è identica). Il body va in `dangerouslySetInnerHTML`. I token Auth0, compreso il refresh token, stanno in `localStorage` sullo stesso origin. | Basta un output LLM malevolo (prompt injection futura, per esempio da dati comunali importati o da un editor manuale) per eseguire JS su `/p/...` e leggere da `localStorage` i refresh token di host e admin che hanno usato la console su quel dominio. | Allowlist reale: `DOMPurify` nel FE e `Ganss.Xss.HtmlSanitizer` nel BE (p, h2-h3, ul/ol/li, strong/em, a[href^=https] con `rel="nofollow noopener"`, table). Test con payload OWASP. Valutare `cacheLocation:'memory'`. | S |
| A8-09 | P1 | FE `index.html:2,7`; `src/lib/seo-meta.ts:29-49`; `compliance-guide-page.tsx:34-43`; `src/routes/catch-all-redirect.tsx:12`; `src/main.tsx:4-10` | **Le pagine SEO sono solo client-side, con metadati minimi.** L'HTML iniziale ha `lang="en"`, titolo `temp-vite`, nessuna description. Titolo e meta arrivano solo dopo JS + fetch API. Mancano `og:url`, `og:image`, `og:type`, JSON-LD (Article/FAQ/BreadcrumbList) e `noindex` sulle pagine "non trovato". `useSeoMeta` non ripristina i meta, per cui il canonical resta sulle pagine successive della SPA. | Crawler senza JS e anteprime social (WhatsApp/Facebook) mostrano "temp-vite". Un comune inesistente risponde 200 con "Pagina non trovata" (soft-404). Un sottopath errato (`/p/affitti-brevi/lombardia`) finisce su `/login`. | Prerender statico delle pagine `/p/*` al build (script che legge le pagine Reviewed dall'API e scrive HTML con head e body) o Vercel Edge Function. `lang="it"` e titolo di default. `noindex` su not-found/error. Cleanup dei meta nell'effetto. JSON-LD. Rimozione dei `console.log` di debug in `main.tsx`. | L |
| A8-10 | P1 | BE (manca l'endpoint); `Casazen.Core/DTOs/PublicPropertyDto.cs:5-25` | **AC2 assente.** Nessuna lista di immobili in evidenza per comune. Il DTO pubblico non ha `OrgSlug` né lo slug host, quindi non si può costruire il deep link al sito booking. | La pagina SEO non porta prenotazioni: nessuna card, nessun link a `/book/...`. | `GET /api/public/seo/{comuneSlug}` che riusa `GetSearchQueryable` (filtro `ComplianceStatus.Active`) con match su `ItalianComuneRegistry.BuildEquivalenceSet`, e aggiunta di `OrgSlug`/`PropertySlug` ai DTO. | M |
| A8-11 | P1 | BE/FE (mancano) | **AC3, AC8 e AC9 assenti:** niente `SeoEvent`, endpoint eventi, analytics o widget admin. | Impossibile misurare quali comuni convertono (User Story "CasaZen growth"). | Entity `SeoEvent {Id, Event, ComuneSlug, UtmSource/Medium/Campaign, ReferrerHost, CreatedAt}` senza IP (hash giornaliero solo se serve dedup, previo parere compliance) + migrazione `AddSeoEvents`. `POST /api/public/seo/events` anonimo con rate limit per IP. `useSeoEvent` con `navigator.sendBeacon`. `GET /api/admin/seo/top-comuni?days=30`. Plausible opzionale via `VITE_PLAUSIBLE_DOMAIN`. | M |
| A8-12 | P1 | FE `compliance-guide-page.tsx:60-63`; `tourist-tax-calculator-widget.tsx:151-155`; BE `SeoContentService.cs:59-61,265-286` | **AC6: la tariffa mancante non è gestita.** Il calcolatore resta visibile e fallisce con "Verifica i dati inseriti" (fuorviante). La sitemap include pagine `TouristTaxCalc` di comuni senza tariffa. Nessun seed di tariffe. | Su `/p/tassa-soggiorno/palermo` senza `TouristTaxRate` ogni calcolo dà 404. L'utente pensa di aver sbagliato i dati. Pagina indicizzata inutile. | Se `touristTaxRate == null`: box di avviso ("Tariffa non ancora disponibile, verifica sul sito del Comune") con link, calcolatore nascosto e pagina esclusa da sitemap o `noindex`. Seed delle tariffe per i 3 comuni pilota. | S |
| A8-13 | P1 | FE `src/features/search/components/search-filters.tsx:54-56,65-116`; `schemas/search.schema.ts:5-10`; `search-page.tsx:24-26,31`; `src/api/properties.api.ts:29-35`; `PublicSiteShell.tsx:120-121` | **La ricerca pubblica è rotta.** Con `valueAsNumber` un input vuoto vale `NaN` e `z.number().optional()` lo rifiuta. L'errore compare solo per `city`, quindi "Cerca" non fa nulla finché non si compilano tutti e 6 i campi numerici. "Dettagli" esegue solo `console.log`. 5 filtri su 8 non vengono inviati all'API. Una pagina pubblica usa `AppShell` della console host (sidebar e bottom-nav host per un anonimo). | Dal link "Esplora" della pagina SEO: si scrive "Como", si preme "Cerca" e non succede nulla. Anche quando i risultati appaiono, il click su "Dettagli" non porta da nessuna parte. | Zod `z.coerce.number().optional()` o `setValueAs` che mappa `''` a `undefined`. Mostrare tutti gli errori. Inviare solo i filtri supportati o estendere l'API. Card collegata a `/book/{orgSlug}/property/{slug}` (dipende da A8-10). Usare `PublicSiteShell`. | M |
| A8-14 | P1 | FE `src/i18n/locales/it.json:1205`; `service-request-form.tsx:207-240`; BE `AiSupplierDiscoveryService.cs:35-56`; `DeepSeekWebSearchClient.cs:31` | **Dati LLM presentati come "più votati su Google".** Il testo dice "Ecco le attività locali più votate su Google" ma nome, telefono, rating e recensioni sono estratti da un LLM. Il supporto del tool `web_search_20250305` sull'endpoint compat Anthropic di DeepSeek non è verificato: se viene ignorato, i dati sono allucinati. | L'host chiama un numero inesistente o di un'altra attività, oppure si fida di un rating inventato: danno reputazionale e possibile pubblicità ingannevole. | Etichetta "Suggerimenti generati da AI — da verificare", fonte e link verificabili (solo `https://`), nessun rating se non c'è una fonte. Oppure disattivare (freeze US-014, vedi A8-16). | S |
| A8-15 | P1 | BE `SupplierMatchService.cs:108-119` (riga 114 `Note host`); `appsettings.json:85-94` | **Dati verso un provider AI extra-UE non dichiarato.** Le note libere dell'host (che possono contenere nomi o orari degli ospiti) finiscono nel prompt DeepSeek. DeepSeek non compare nella lista subprocessori mostrata in onboarding (`/api/legal/subprocessors`). | Un host scrive "ospite Mario Rossi arriva alle 15, chiamare 333…": il dato personale va a un subprocessore non dichiarato né coperto da DPA o valutazione del trasferimento (GDPR cap. V). | Togliere `notes` dal prompt (la motivazione non ne ha bisogno) o anonimizzarle. Aggiungere il provider AI alla lista subprocessori con valutazione del trasferimento, oppure passare a un provider con region UE. | S |
| A8-16 | P1 | FE `route-manifest.ts:318-334,359-384`; `ota-setup-page.tsx:128-143`; `onboarding-page.tsx:20`; BE `OtaIntegration.cs:26,30`; `Program.cs:381-391,417-433` | **Feature in freeze esposte.** Il menu OTA chiede API key e secret di Airbnb/Booking (API partner-only, freeze #31-35), salvati in chiaro. I job OTA girano ogni 15 min e ogni ora con `Guid.Empty` (no-op + warning). LTR è selezionabile in onboarding e completamente navigabile, con sviluppo attivo ad agosto 2026. Discovery AI esterno (US-014) attivo. | L'host inserisce credenziali Airbnb convinto di sincronizzare: non succede nulla e i segreti restano in chiaro nel DB. I log Hangfire si riempiono di "OTA sync completed with errors for property 0000…". | Feature flag BE (`Features:OtaApi`, `Features:LongTerm`, `Features:AiSupplierDiscovery`) esposti via `/api/me` o config, che filtrano `ROUTE_MANIFEST`, le opzioni di onboarding e la registrazione dei job ricorrenti. Cifrare `OtaIntegration.ApiKey/ApiSecret` (Data Protection) o eliminarli. | M |
| A8-17 | P2 | FE `it.json:914,935,1005,1129`; BE `PricingAdapterService.cs:27,147,149`; `docs/AI-STRATEGY.md:14-30` | **"AI Dynamic Pricing" è un moltiplicatore fisso** (×1.5 festivi, ×1.3 estate, ×0.8 inverno). | Claim di marketing non veritiero, rilevato dagli stessi documenti interni. | Rinominare in "Prezzi dinamici (regole)" finché non esiste un modello reale. | S |
| A8-18 | P2 | FE `e2e/compliance-journey.spec.ts:161-191`; commit FE `7b53a08`; BE `Casazen.Tests/Integration/ComplianceSeoIntegrationTests.cs:26-34`; `SeoContentServiceTests.cs:66-105` | **Test deboli o assenti.** L'E2E SEO dedicato è stato cancellato. L'E2E sulla tassa non mocka `GET /api/public/content/tassa-soggiorno/como` e quindi dipende da un backend reale (nel run L2 demo va in "not found"). Il test BE `AC2_…Returns404…` verifica 200. Nessun test controlla che in Production una Draft dia 404, né i test del sanitizer, del canonical/host o dell'uso token DeepSeek. Nessun test FE per CTA, guida o dashboard admin. | Regressioni come A8-03, A8-05 e A8-08 passano la CI senza essere notate. | AC Test Map per US-026. Test L1: gating Production, sanitizer (payload OWASP), budget (`Times.Never`), eventi. Test L2 titolati per AC con `page.route` completo. L3 su staging per CTA → signup. | M |
| A8-19 | P2 | FE `tourist-tax-calculator-widget.tsx:43-44`; `seo-dashboard-page.tsx:119`; `it.json:2185-2197,2190`; `protected-route.tsx:25,37`; `legacy-redirect.tsx:14`; BE `SeoContentService.cs:89,437-453,482-492`; `PublicContentController.cs:65`; `AdminSeoController.cs:122` | **i18n.** Nel FE sono hardcoded "/persona/notte" e "max N notti". Il tipo pagina è mostrato come enum grezzo ("ComplianceGuide"). "e'" e "conformita'" al posto di è/à. Manca il plurale ("1 notti"). Sul percorso del funnel compaiono stringhe inglesi ("Authenticating...", "Loading workspace..."). Nel BE disclaimer, titoli e messaggi non passano da `IStringLocalizer`. L'errore 400 è in inglese e il 403 espone `ex.Message`. | L'utente EN vede i disclaimer in italiano. L'utente IT vede "Invalid calculation parameters." e "[COUNSEL_REQUIRED] First 100 SEO pages…". | Chiavi `t()` e `.resx`, `Intl.PluralRules`, correzione degli accenti, messaggi d'errore localizzati senza dettagli interni. | S |
| A8-20 | P2 | FE `package.json` (manca `@tailwindcss/typography`); `src/styles/globals.css:1`; `compliance-guide-page.tsx:55` | **La classe `prose` non ha effetto:** l'HTML del contenuto non ha stili (h2 e ul appiattiti dal preflight). | Anche un contenuto AI di qualità appare come un blocco di testo senza gerarchia. | `@plugin "@tailwindcss/typography"` o stili dedicati. | S |
| A8-21 | P2 | FE `seo-dashboard-page.tsx:28,57,74-86,109-150`; BE `AdminSeoController.cs:26-30`; `SeoContentRepository.cs:85` | **UX admin SEO.** Mancano anteprima, link alla pagina pubblica e azione "Ritira" (torna Draft). Paginazione fissa alla pagina 1 da 50. Il budget non ha stato di caricamento o errore. "Approva tutte" parte senza conferma. `void mutateAsync` genera promise rejection non gestite. Il BE non valida `page` e `pageSize` (`page=0` → OFFSET negativo → 500). | Una pagina legale errata non si può ritirare dalla UI. Un click accidentale su "Approva tutte" pubblica tutto. | Colonna azioni con Anteprima, Ritira e Apri pubblica. Dialog di conferma. Paginazione. Validazione `[Range]` sui parametri. | S-M |
| A8-22 | P2 | BE `Program.cs:153-158` | **Il rate limiter `PublicTouristTaxCalc` è globale** (`AddFixedWindowLimiter`), non per IP: 30 richieste al minuto per tutto il sito. | Un singolo bot esaurisce la quota e tutti gli utenti ricevono 429 per quel minuto. | `AddPolicy` partizionato per IP (`GetClientIpRateLimitKey`, già usato per `PublicResolveHost`). | S |
| A8-23 | P2 | BE `TouristTaxService.cs:40-41`; `TouristTaxRateRepository.cs:26`; `SeoContentRepository.cs:127` | **Calcolo tassa impreciso.** I bambini sono sempre esenti a prescindere da `MinimumAge` e la UI non chiede l'età. Il lookup `t.City == city` è case-sensitive su PostgreSQL, mentre il refresh usa `OrdinalIgnoreCase`. | Un ragazzo di 12 anni a Roma (esenzione under 10) viene conteggiato come esente: stima sbagliata. Un admin che inserisce "roma" ottiene "calcolatore non disponibile" sulla pagina di Roma, anche se il refresh la considera obsoleta. | Chiedere l'età dei minori o distinguere esenti e paganti. Normalizzare la città (ISTAT code FK) o usare `EF.Functions.ILike`. | S |
| A8-24 | P2 | BE `ItalianComuneRegistry.cs:13-26` | **Registry dei comuni hardcoded (12) con codici ISTAT dubbi.** Torino ha `010025`, che è il codice di Genova (Torino = `001272`). Da verificare anche Bellagio (`013250` dopo la fusione 2014), Menaggio e Varenna. | `ComuneCode` sbagliato nelle pagine e nei join futuri con dataset ISTAT o con le tariffe per codice. | Correggere i codici e caricare il registry da tabella/seed ISTAT (lo scale a 1.409 comuni è fuori scope, ma serve la struttura). | S |
| A8-25 | P2 | BE `DeepSeekAiProvider.cs:18,26-27,55`; `AiSupplierDiscoveryService.cs:15,51-54`; `SupplierMatchService.cs` (cacheKey con hash note); `SeoContentService.cs:322` | **Cache AI statiche illimitate e con semantica sbagliata.** La cache del provider è permanente, senza TTL né limite, e ha chiavi con l'hash delle note: la memoria cresce senza limite. Il discovery ha TTL di 24 h, ma l'estrazione resta in cache per sempre con chiave solo città/categoria. `forceRegenerate` SEO riceve il contenuto dalla cache. | Dopo 24 h si ripaga la web search ma si ottiene l'estrazione vecchia. Il pulsante "rigenera" non rigenera. La memoria del processo cresce a ogni richiesta con note diverse. | `IMemoryCache` con `SizeLimit` e TTL. Bypass della cache con `forceRegenerate`. Chiave dell'estrazione basata sull'hash del contenuto cercato. | S |
| A8-26 | P2 | BE `SeoBootstrapOptions.cs:14`; `appsettings.json:100`; `Program.cs:103`; `ServiceCollectionExtensions.cs:364`; `SeoBootstrapHostedService.cs:27-38` | **Config morta o duplicata.** `Seo:AiProvider` non viene mai letto (il provider reale è `Ai:Provider`). `AddCasazenAiProvider` è registrato due volte. Il bootstrap riaccoda la generazione a ogni avvio finché ci sono 0 pagine e, con più repliche, duplica il job. | Chi configura `Seo:AiProvider=DeepSeek` pensa di aver attivato l'AI e resta con lo Stub. Una generazione fallita viene ripetuta (e pagata) a ogni deploy. | Rimuovere la chiave morta e la seconda registrazione. Lock distribuito o flag "bootstrap done" persistito. | S |
| A8-27 | P2 | FE `src/components/shared/ai-content-notice.tsx:8-15` (usato solo in `__tests__`) | **Il componente di trasparenza AI Act non è montato** in nessuna pagina (motivazioni AI, discovery, contenuti SEO). | La trasparenza dichiarata non viene mostrata agli utenti. | Montarlo accanto a ogni contenuto generato e rimuovere la duplicazione del disclaimer. | S |
| A8-28 | P2 (cross-area) | FE `src/features/compliance/activation-wizard.tsx:198,264`; `routes/index.tsx:169` | **Lo step tassa del wizard linka una rotta admin** (`/app/admin/compliance/tax-rates`) inaccessibile all'host, invece di mostrare avviso e link alla pagina SEO come da PLANNING.md:262. Il link CIN `https://www.casazen.app/help/cin` non ha rotta nella SPA (esiste solo `/help/ical`). | L'host clicca e viene rediretto al proprio contesto o a una pagina inesistente. | Link a `/p/tassa-soggiorno/{slug}` o al sito del Comune. Creare `/help/cin` o usare `Compliance:CinGuidanceUrl` (`appsettings.json`). | S |
| A8-29 | P1 (cross-area) | FE `src/features/public-booking/*` (nessun `useSeoMeta`/JSON-LD); BE nessun endpoint sitemap org; spec `spec-branded-booking-site.md:76-97` | **La destinazione del funnel non è indicizzabile.** AC12-14 (JSON-LD `VacationRental`, meta/og, sitemap e robots per org) mancano nonostante lo stato "shipped". | `/book/{slug}` e le pagine immobile hanno titolo "temp-vite" e nessuna anteprima social: la conversione organica verso il booking diretto è nulla. | Stesso meccanismo di prerender/meta di A8-09, JSON-LD dal read-model pubblico, `GET /api/public/orgs/{slug}/sitemap.xml`. | M |
| A8-30 | P2 | BE `Sessions/specs/spec-seo-funnel.md:8,149-151`; `Sessions/specs/README.md:85,123`; `Sessions/PLANNING.md:201` | **Documentazione incoerente.** La spec ha `status: specced` e `issue:` vuoto, mentre il registro indica `planned #300`. Le Technical Notes citano file inesistenti (`PublicSeoController.cs`, `features/seo/ComuneLandingPage.tsx`). PLANNING dichiara "Backend OK". | Chi implementa lavora sui file sbagliati e parte dall'assunto falso che il BE sia pronto. | Allineare la spec ai file reali (`PublicContentController.cs`, `src/features/public-seo/*`), aggiungere la AC Test Map e aggiornare PLANNING. | S |

**Verifiche con esito positivo (nessun difetto):**
- Tutti gli endpoint admin SEO hanno `[Authorize(Policy = "AdminOnly")]` (`AdminSeoController.cs:14`) e la policy richiede il ruolo `Admin` (`ServiceCollectionExtensions.cs:163`). Il permesso FE `admin.seo.read` è presente nel bootstrap (`ContextAccessBootstrap.cs:54`).
- Le chiamate FE SEO corrispondono a rotte e shape BE: `admin-seo.api.ts` e `public-seo.api.ts` ↔ `AdminSeoController`/`PublicContentController`; `PagedResultDto` ↔ `BackendPagedResult`; enum serializzati come stringa (`Program.cs:211`).
- I path della sitemap e dei canonical (`/p/affitti-brevi/{region}/{comune}`, `/p/tassa-soggiorno/{comune}`) corrispondono alle rotte FE (`routes/index.tsx:144-145`): il problema è l'host, non il path.
- Nessuna chiave reale committata (`appsettings.json` contiene solo placeholder). `Ai:ApiKey` non viene loggata né restituita.
- Tutte le chiavi `t()` usate in `public-seo`, `seo-dashboard-page` e `search` esistono sia in `it.json` sia in `en.json` (verifica con script).
- Il mobile (`/home/user/mobile`) non espone codice SEO, AI o freeze.

---

### 3. Piano di fix dell'area

Ordine con dipendenze. Gli effort sono cumulativi per passo.

**Fase 0: contenimento (1-2 giorni, nessuna dipendenza, da fare subito)**
1. **Stop costi AI incontrollati (A8-01, A8-07):** allowlist delle categorie, rate limit per utente/org su `match-supplier`, `AiBudgetGuard` centrale con controllo prima della chiamata e parsing di `usage` DeepSeek, `break` del batch SEO. Flag `Features:AiSupplierDiscovery=false` in prod finché US-014 è in freeze. *(M)*
2. **Stop pubblicazione non revisionata (A8-04, A8-05, A8-06):** `Seo__BootstrapOnStartup=false` e `Seo__AutoApproveAfterBootstrap=false` su Railway. Rimuovere i default `counselApproved=true` (BE `SeoAdminDtos.cs:5`, FE `admin-seo.api.ts:45`, `use-admin-seo.ts:28`, `seo-dashboard-page.tsx:44,140`). Script o azione admin per rimettere in Draft le pagine con body stub o vuoto. Rigenerazione → Draft. *(S-M)*
3. **Sicurezza HTML (A8-08):** DOMPurify nel FE e HtmlSanitizer allowlist nel BE, con test sui payload. *(S)*
4. **GDPR AI (A8-15, A8-14):** togliere le note dal prompt, aggiornare la lista subprocessori, correggere il testo "Google". *(S)*
5. **Freeze (A8-16):** feature flag che nascondono OTA API, LTR (onboarding + contesto) e discovery AI, più deregistrazione dei job `ota-sync-all`/`booking-pull-all`. Decidere se cifrare o eliminare `OtaIntegration.ApiKey/ApiSecret`. *(M)*

**Fase 1: indicizzabilità (dipende da una decisione di prodotto sul dominio canonico)**
6. **ADR dominio canonico** (per esempio `casazen.app` o `www.casazen.it`): impostare `Seo__PublicBaseUrl`, CORS e dominio Vercel, documentare in `docs/INFRA.md`. *(S, bloccante per 7-8)*
7. **robots.txt + sitemap sul dominio FE**, pagina hub `/p/affitti-brevi` e link dal footer pubblico (A8-02). *(S-M, dipende da 6)*
8. **Prerender/SSG delle pagine `/p/*`** e meta di base (`lang="it"`, titolo, og, JSON-LD, `noindex` su not-found, cleanup `useSeoMeta`), più typography (A8-09, A8-20). Riusare lo stesso meccanismo per `/book/*` (A8-29). *(L, dipende da 6; parallelizzabile con 9-11)*

**Fase 2: funnel US-026 (#300) (dipende da Fase 0 e da 6)**
9. **Signup reale (A8-03):** rotta pubblica `/signup` con `screen_hint=signup` e `appState` (comune + utm), persistenza dell'attribuzione in onboarding (migrazione), rimozione della CTA "Verifica conformità" o sua sostituzione con una pagina minima. *(M)*
10. **BE AC2 + AC3 (A8-10, A8-11):** `OrgSlug`/`PropertySlug` nel DTO pubblico, `GET /api/public/seo/{comuneSlug}` con immobili in evidenza, entity e migrazione `SeoEvent` senza PII (parere compliance su IP come da README.md:123), `POST /api/public/seo/events` con rate limit per IP. *(M)*
11. **FE AC4-AC8:** rewrite della pagina con i token di `PublicSiteShell`, CTA "Pubblica la tua casa" → `/signup?comune=`, card immobili → `/book/{orgSlug}/property/{slug}`, `useSeoEvent` + Plausible via env, avviso tariffa mancante (A8-12). *(M, dipende da 9 e 10)*
12. **AC9:** endpoint `top-comuni` e widget nella dashboard admin. *(S, dipende da 10)*
13. **Contenuti pilota AC10 (A8-06, A8-24):** prompt di sistema versionato con validazione dell'output, anteprima e ritiro nell'admin, audit dell'approvazione (A8-21), correzione dei codici ISTAT, seed delle tariffe per 3 comuni pilota (per esempio Como, Milano, Roma), immobili demo `Active`. *(M-L, dipende da 2 e 10)*

**Fase 3: qualità e superfici collegate**
14. **Ricerca pubblica (A8-13):** fix NaN/zod, link ai dettagli, shell pubblico. *(M, dipende da 10 per lo slug)*
15. **Calcolatore (A8-22, A8-23)** e **wizard compliance → pagina SEO (A8-28)**. *(S)*
16. **i18n (A8-19), cache (A8-25), config (A8-26), AI Act notice (A8-27), rinomina "Prezzi AI" (A8-17), documenti (A8-30).** *(S ciascuno)*
17. **Test (A8-18):** AC Test Map US-026, L1 (gating Production, sanitizer, budget, eventi), L2 titolati per ogni AC, L3 staging CTA → signup; ripristino di un E2E SEO con tutti i mock. *(M, in parallelo a ogni passo)*

Stima totale dell'area: circa 15-22 giornate/uomo (Fase 0 ≈ 3-4 g; Fase 1 ≈ 4-6 g; Fase 2 ≈ 6-8 g; Fase 3 ≈ 3-4 g).

---

### 4. Verdetto

**Completamento reale stimato: circa 25-30 %.** In dettaglio: funnel US-026 circa 20 % (1 AC su 10 soddisfatto come prerequisito, 3 parziali, 1 rotto, 5 mancanti); base SEO #258 circa 50 % sul piano funzionale ma non affidabile in produzione; indicizzabilità effettiva circa 10 %; funzioni AI circa 30 %, senza cap dei costi.

Esistono entity, API pubbliche, job e dashboard admin, ma il percorso che dà valore (Google → pagina comune → CTA → signup attribuito o prenotazione) si interrompe in tre punti:
- le pagine non sono scopribili sul dominio reale (canonical e sitemap su `www.casazen.it`, sitemap solo sull'host API, nessun robots.txt, rendering solo client con titolo "temp-vite");
- la CTA porta al login e perde l'attribuzione;
- non esistono immobili in evidenza né tracciamento.

Il contenuto pubblicato di default è una frase dello Stub, auto-approvata aggirando il gate di revisione legale. Con DeepSeek il budget non conta i token e l'endpoint `match-supplier` permette spesa AI illimitata a qualunque utente registrato.

Sul fronte freeze, US-011, US-012, US-015, US-016 e US-017 sono correttamente non iniziate. OTA API e LTR invece sono esposte agli utenti e LTR ha ricevuto sviluppo attivo ad agosto 2026, mentre il funnel SEO (P0 MVP) non è partito. Le priorità vanno riallineate con i feature flag della Fase 0 prima di proseguire.
