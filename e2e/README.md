# E2E path anchors (backend repo)

Stage 02 `check-ac-matrix.ps1` requires L2/L3 path tokens to exist on disk.
Canonical Playwright specs live in `casazen/frontend`. Files here are scaffolds /
mirrors for G9 path-exists during delivery ticks on the backend checkout.

Do not treat these as the FE source of truth — Stage 03 FE PRs own the real specs.
