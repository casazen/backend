# D-AC6

## Iteration 1 — dev
Maestro yaml M1–M7 now set `EXPO_PUBLIC_API_URL=http://localhost:5000` (removed `EXPO_PUBLIC_E2E_DEMO=1`). M3 uses `casazen://bookings/${BOOKING_ID}`.

## Review
STATO: APPROVED
Shared real-API seed path is declared. Live execution is a separate env discrepancy (D-M-LIVE).
