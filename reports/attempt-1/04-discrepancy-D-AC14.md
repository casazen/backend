# D-AC14

## Iteration 1 — dev
L3 golden-journey-web reads `GET /api/service-requests/{id}` and `GET /api/bookings/{id}` after steps 7–10 and asserts both `status` fields are present (same API both clients use).

## Review
STATO: APPROVED
Parity is asserted on the shared real API. Cross-binary app GET still needs Maestro (D-M-LIVE).
