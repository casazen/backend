# Review — D-AC8-EXPORT

STATO: APPROVED

Export is wired: DI registers `IComuneImuNotificationService`; `GET /api/leases/{id}/canone-concordato/imu-notification/export` returns `%PDF` / 409 / 404; Seveso PDF keeps channel uncertainty; Cesano labels `valore derivato`. L1 tests in `ComuneImuNotificationServiceTests` passed in the 28-test run.
