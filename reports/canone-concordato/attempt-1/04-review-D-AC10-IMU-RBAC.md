# Review — D-AC10-IMU-RBAC

STATO: APPROVED

Export inherits class `RequireContext:long-rent:lease.read`. Mark-sent adds `lease.register`. Neither uses `property.read`. Other owner → 404 on both. Verified by `Controller_Export_UsesClassLeaseRead_NotPropertyRead` and `Controller_MarkSent_RequiresLeaseRegisterPolicy`.
