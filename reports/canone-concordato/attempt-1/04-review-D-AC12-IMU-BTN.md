# Review — D-AC12-IMU-BTN

STATO: APPROVED

`imu-notification-export-button.tsx` mounts on lease detail inside the existing long-rent shell. Buttons disabled unless `leaseStatus === 'Registered'`. Italian labels via `t('leases.canoneConcordato.exportImu')`.
