# Review — D-AC10-LIST + D-AC9-SEED + D-AC13 + Maestro

STATO: APPROVED

Host list filters by `bookingId`. L3 attaches the SR to the booking. F1–F2 take/complete is required and confirmed via GET. Maestro M1–M7 used the L3 `BOOKING_ID` with required status / checkout asserts. Booking-detail cache invalidation makes M4 `Richiesto` observable.
