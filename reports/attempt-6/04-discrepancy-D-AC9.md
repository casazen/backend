# Review — D-AC9 picker + L3 seed

STATO: APPROVED

Empty-category Active suppliers are now returned by `GetActiveByComune`. L3 links the host token to a supplier org, activates, and asserts Richiesto → PresoInCarico → Completato → Pagato. Unique-address create returns 409 (unit tests passed).
